apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-fleet
  labels:
    app: fleet
spec:
  type: {{ .Values.fleetdm.service.type }}
  selector:
    app: fleet
  ports:
    - port: {{ .Values.fleetdm.service.port }}
      targetPort: {{ .Values.fleetdm.service.port }}
